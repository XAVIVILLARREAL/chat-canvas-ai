name: graph_core
description: Modelos puros del grafo de proyecto (nodos, aristas, indexador de relaciones y simulacion de fuerzas).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
