name: warp_core
description: Historial de comandos por host + búsqueda fuzzy + tracker de línea (Warp-mode, Etapa 8.5). Dart puro.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  ssh_core:
    path: ../ssh_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
