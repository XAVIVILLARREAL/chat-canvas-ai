name: spatial_core
description: Índice espacial quad-tree en Dart puro (query por rect/círculo) para canva LOD y futuros usos.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
