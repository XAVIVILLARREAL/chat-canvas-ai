name: agent_core
description: Modelo puro de agentes IA y detección de estados (portado de herdr, Apache-2.0).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies: {}

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
