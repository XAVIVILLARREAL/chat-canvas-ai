name: canva_core
description: Modelo puro del canva (nodos, edges, IDs) sin dependencias de Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  agent_core:
    path: ../agent_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
