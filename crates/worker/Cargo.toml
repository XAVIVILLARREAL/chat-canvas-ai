[package]
name = "canvas-ai-worker"
version = "0.1.0"
description = "Worker stateless de agentes (patrón Everruns, SDD-008) — SIN credenciales de DB; reclama tareas con FOR UPDATE SKIP LOCKED + heartbeat"
edition = "2021"

[dependencies]
canvas-ai-core = { path = "../core" }
tokio.workspace = true
serde_json.workspace = true
