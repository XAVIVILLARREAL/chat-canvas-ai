name: empresa_dev_monorepo
description: Monorepo de Empresa Dev — app Flutter (apps/empresa_dev) + librerías puras (packages/*).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

# Melos v3+ requiere un pubspec local; es el workspace raíz (no se publica).
dev_dependencies:
  melos: ^6.3.3
